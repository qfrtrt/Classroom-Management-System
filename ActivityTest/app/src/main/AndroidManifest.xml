<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a94936.activitytest">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FristActivity"
            android:label="登录">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:label="查询" />
        <activity
            android:name=".queryshowActivity"
            android:label="查询" />
        <activity
            android:name=".orderActivity"
            android:label="学生预约" />
        <activity
            android:name=".SuccessActivity"
            android:label="操作成功" />
        <activity android:name=".Administartor" />
        <activity android:name=".teacher" />
        <activity android:name=".backreasonActivity"></activity>
    </application>

</manifest>